package com.nibado.projects.advent

import java.util.*

object Primes {
//    lateinit val bitSet: BitSet
//
//    private fun calculate(max: Int) : BitSet {
//        val primeMap = BitSet(max / 2)
//        primeMap.set(0, primeMap.size() - 1)
//
//        for(i in 3 .. max step 2) {
//            val index = i / 2
//            if(primeMap[index]) {
//                for(j in index .. max step i)
//            }
//        }
//    }
}